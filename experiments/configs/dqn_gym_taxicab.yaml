# Experiment hyper-parameters
n_epochs: 400
# Update steps in epoch
epoch_steps: 1000
# Max steps in episode
max_steps: 200
buffer_size: 20000
seed: 1

# Algorithm hyper-parameters
step_size: 0.001 #alpha
disc_fact: 0.99 #gamma
batch_size: 64
warmup_steps: 500
target_update_steps: 500
algo_type: dqn
hidden_dim: null

schedule_start: 1
schedule_end: 0.05
schedule_fraction: 0.6
